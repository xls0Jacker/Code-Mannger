describe("multiplyNumeric", function() {
    it("width is double", function() {
        assert.equal(getWidth(),400);
    });
    it("for non-integer n the result is NaN", function() {
        assert.equal(getHeight(),600);
    });
});